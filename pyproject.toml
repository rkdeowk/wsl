[project]
name = "my-project"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []

[project.optional-dependencies]
dev = ["pre-commit"]

[tool.ruff]
line-length = 100
target-version = "py312"
